{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "run and open Chrome",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:3000",
            // "webRoot": "${workspaceRoot}/client/src",
            "preLaunchTask": "npm: start",

        },
        {
            "name": "Chrome",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceRoot}/client/src",
        }
    ]
}